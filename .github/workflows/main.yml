name: 跨平台编译 (Fyne)

on:
  push:
    tags:
      - 'v*' # 只有推送到 v1.0.0 这种标签时才触发发布

jobs:
  release:
    runs-on: ubuntu-latest # Fyne-cross 在 Linux 环境下运行最稳
    steps:
      - uses: actions/checkout@v4

      - name: 设置 Go 环境
        uses: actions/setup-go@v5
        with:
          go-version: '1.25' # 根据你的项目调整版本

      # 使用 fyne-cross 进行交叉编译
      # 使用 fyne-cross 进行交叉编译，添加 -app-id 参数
      - name: 编译多平台二进制文件
        run: |
          go install github.com/fyne-io/fyne-cross@latest
          
          # 注意：把 com.yourname.myproxy 换成你自己的标识
          # 编译 Windows
          fyne-cross windows -arch=amd64 -app-id com.lucastq.myproxy
          
          # 编译 Linux
          fyne-cross linux -arch=amd64 -app-id com.lucastq.myproxy
          
          # 编译 macOS
          fyne-cross darwin -arch=amd64 -app-id com.lucastq.myproxy

      # 自动创建 GitHub Release 并上传文件
      - name: 发布到 Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            fyne-cross/dist/windows-amd64/*.zip
            fyne-cross/dist/linux-amd64/*.tar.xz
            fyne-cross/dist/darwin-amd64/*.app.tar.xz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
